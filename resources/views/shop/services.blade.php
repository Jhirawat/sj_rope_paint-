@extends('layouts.app')
@section('content')<section class="section"><div class="container"><h1 class="fw-bold mb-4">บริการ</h1><div class="row g-4">@foreach($services as $service)<div class="col-md-6"><div class="card card-sj h-100">@if($service->image)<img src="{{ asset('storage/'.$service->image) }}" class="service-cover" alt="{{ $service->title() }}">@endif<div class="p-4"><h4>@if($service->icon)<i class="{{ $service->icon }} text-warning me-2"></i>@endif{{ $service->title() }}</h4><p>{{ $service->excerpt() }}</p><a href="{{ route('services.show',$service) }}" class="btn btn-sj align-self-start">ดูรายละเอียด</a></div></div></div>@endforeach</div></div></section>@endsection
