@extends('layouts.app')
@section('content')<section class="section"><div class="container"><div class="row g-5"><div class="col-lg-8">@if($service->image)<img src="{{ asset('storage/'.$service->image) }}" class="w-100 rounded-4 mb-4" style="max-height:420px;object-fit:cover" alt="{{ $service->title() }}">@endif<h1 class="fw-bold">{{ $service->title() }}</h1><p class="lead text-muted">{{ $service->excerpt() }}</p><div class="card card-sj p-4">{!! nl2br(e(app()->getLocale()==='en' && $service->content_en ? $service->content_en : $service->content_th)) !!}</div></div><div class="col-lg-4"><div class="card card-sj p-4"><h5>สนใจบริการนี้?</h5><a href="{{ route('quote.form',['service_id'=>$service->id]) }}" class="btn btn-sj">ขอใบเสนอราคา</a></div></div></div></div></section>@endsection
