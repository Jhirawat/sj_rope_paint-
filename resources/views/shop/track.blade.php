@extends('layouts.app')
@section('title', app()->getLocale()==='en'?'Track Request':'ติดตามสถานะงาน')
@section('content')
<section class="section bg-light"><div class="container"><div class="row justify-content-center"><div class="col-lg-7"><div class="card card-sj p-4"><h1 class="h3 fw-bold mb-3">{{ app()->getLocale()==='en'?'Track your request':'ติดตามสถานะงาน' }}</h1><p class="text-muted">{{ app()->getLocale()==='en'?'Enter tracking code and phone number. No login required.':'กรอกรหัสงานและเบอร์โทร ไม่ต้องสมัครสมาชิก' }}</p><form method="post" action="{{ route('quote.track') }}">@csrf<div class="mb-3"><label class="form-label">{{ app()->getLocale()==='en'?'Tracking Code':'รหัสงาน' }}</label><input class="form-control" name="quotation_no" placeholder="SJQ-20260617-0001" required></div><div class="mb-3"><label class="form-label">{{ app()->getLocale()==='en'?'Phone':'เบอร์โทร' }}</label><input class="form-control" name="phone" required></div><button class="btn btn-sj w-100">{{ app()->getLocale()==='en'?'Check Status':'ตรวจสอบสถานะ' }}</button></form>@isset($searched)<hr>@if($quotation)<h5>{{ $quotation->quotation_no }}</h5><div class="alert alert-info mb-0"><b>{{ app()->getLocale()==='en'?'Status':'สถานะ' }}:</b> {{ $quotation->status }}</div>@else<div class="alert alert-warning mb-0">{{ app()->getLocale()==='en'?'No matching request found.':'ไม่พบข้อมูลที่ตรงกัน' }}</div>@endif@endisset</div></div></div></div></section>
@endsection
