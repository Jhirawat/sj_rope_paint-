@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between mb-3"><h1>ใบงาน</h1><a href="{{ route('admin.dashboard') }}" class="btn btn-outline-secondary"><i class="fa-solid fa-calendar-days"></i> ดูแจ้งเตือนงานใกล้ถึงกำหนด</a></div>
<div class="card p-3"><table class="table align-middle"><tr><th>เลขที่</th><th>ลูกค้า/สถานที่</th><th>ติดต่อด่วน</th><th>กำหนดงาน</th><th>สถานะ</th><th></th></tr>@foreach($workOrders as $w)<tr><td><b>{{ $w->work_order_no }}</b><br><small class="text-muted">{{ optional($w->service)->title_th }}</small></td><td>{{ $w->customer_name }}<br><small>{{ $w->address }}</small></td><td><a class="btn btn-sm btn-success" href="tel:{{ $w->phone }}"><i class="fa-solid fa-phone"></i> โทร</a>@if($w->lineUrl()) <a class="btn btn-sm btn-success" target="_blank" href="{{ $w->lineUrl() }}"><i class="fa-brands fa-line"></i> LINE</a>@endif @if($w->mapUrl()!=='#')<a class="btn btn-sm btn-primary" target="_blank" href="{{ $w->mapUrl() }}"><i class="fa-solid fa-map-location-dot"></i> แผนที่</a>@endif</td><td><small>สำรวจ: {{ $w->scheduled_survey_at?->format('d/m/Y H:i') ?: '-' }}<br>เริ่ม: {{ $w->start_at?->format('d/m/Y') ?: '-' }}<br>จบ: {{ $w->finish_at?->format('d/m/Y') ?: '-' }}</small></td><td>{{ $w->statusText() }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.work-orders.edit',$w) }}"><i class="fa-solid fa-pen"></i> แก้ไข</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.work-orders.show',$w) }}"><i class="fa-solid fa-clipboard-list"></i> ใบงาน</a><a class="btn btn-sm btn-sj" href="{{ route('admin.work-orders.print',$w) }}" target="_blank"><i class="fa-solid fa-print"></i> A4</a></td></tr>@endforeach</table>{{ $workOrders->links() }}</div>
@endsection
