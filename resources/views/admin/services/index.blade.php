@extends('layouts.admin')
@section('content')<div class="d-flex justify-content-between mb-3"><div><h1>บริการ</h1><div class="small-help">รายการที่อยู่บนสุด จะแสดงก่อนในหน้าเว็บไซต์</div></div><a class="btn btn-sj" href="{{ route('admin.services.create') }}"><i class="fa-solid fa-plus"></i> เพิ่มบริการ</a></div><div class="card p-3"><table class="table align-middle"><tr><th>ลำดับ</th><th>รูป</th><th>ชื่อ</th><th>Slug</th><th>สถานะ</th><th>จัดการ</th></tr>@foreach($services as $s)<tr><td><div class="d-flex gap-1 flex-wrap"><form method="post" action="{{ route('admin.services.move-up',$s) }}">@csrf<button class="btn btn-sm btn-outline-secondary"><i class="fa-solid fa-arrow-up"></i> เลื่อนขึ้น</button></form><form method="post" action="{{ route('admin.services.move-down',$s) }}">@csrf<button class="btn btn-sm btn-outline-secondary"><i class="fa-solid fa-arrow-down"></i> เลื่อนลง</button></form></div></td><td>@if($s->image)<img src="{{ asset('storage/'.$s->image) }}" class="thumb">@endif</td><td>{{ $s->title_th }}<br><small>{{ $s->title_en }}</small></td><td>{{ $s->slug }}</td><td>{{ $s->is_active?'แสดง':'ซ่อน' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.services.edit',$s) }}"><i class="fa-solid fa-pen"></i> แก้ไข</a><form method="post" action="{{ route('admin.services.destroy',$s) }}" class="d-inline">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('ลบ?')"><i class="fa-solid fa-trash"></i> ลบ</button></form></td></tr>@endforeach</table>{{ $services->links() }}</div>@endsection
