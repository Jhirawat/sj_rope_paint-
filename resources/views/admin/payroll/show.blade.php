@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">{{ $period->name }}</h1><p class="text-muted mb-0">{{ $period->start_date->format('d/m/Y') }} - {{ $period->end_date->format('d/m/Y') }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.payroll.export',$period) }}" class="btn btn-outline-success">Export CSV</a><form method="post" action="{{ route('admin.payroll.confirm',$period) }}">@csrf<button class="btn btn-outline-primary">ยืนยันรอบจ่าย</button></form><form method="post" action="{{ route('admin.payroll.paid',$period) }}">@csrf<button class="btn btn-sj">จ่ายแล้ว</button></form></div></div>
<div class="card p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>พนักงาน</th><th>ค่าแรง</th><th>แรงรวม</th><th>รวม</th><th>หัก</th><th>เบิก</th><th>สุทธิ</th><th>สาย/เตือน</th></tr></thead><tbody>@foreach($period->items as $i)<tr><td><strong>{{ $i->employee?->name }}</strong></td><td>{{ number_format($i->daily_wage) }}</td><td>{{ $i->work_units }}</td><td>{{ number_format($i->gross_amount) }}</td><td class="text-danger">{{ number_format($i->deduction_amount) }} @if($i->late_count>=3)<div class='small'>สายครบ 3 ครั้ง หัก 1 แรง</div>@endif</td><td class="text-warning">{{ number_format($i->advance_amount) }}</td><td><strong>{{ number_format($i->net_amount) }}</strong></td><td>{{ $i->late_count }} / {{ $i->warning_count }}</td></tr>@endforeach</tbody></table></div></div>
@endsection
