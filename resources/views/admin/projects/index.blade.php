@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between mb-3"><div><h1>ผลงาน</h1><div class="small-help">ใช้ปุ่มเลื่อนขึ้น/ลงแทนการกรอกเลขลำดับ ผู้ใช้งานเข้าใจง่ายกว่า</div></div><a class="btn btn-sj" href="{{ route('admin.projects.create') }}"><i class="fa-solid fa-plus"></i> เพิ่มผลงาน</a></div>
<div class="card p-3"><table class="table align-middle"><tr><th>ลำดับ</th><th>ข้อมูลผลงาน</th><th>รูปภาพ</th><th>สถานะ</th><th></th></tr>
@foreach($projects as $p)<tr><td><div class="d-flex gap-1 flex-wrap"><form method="post" action="{{ route('admin.projects.move-up',$p) }}">@csrf<button class="btn btn-sm btn-outline-secondary"><i class="fa-solid fa-arrow-up"></i> เลื่อนขึ้น</button></form><form method="post" action="{{ route('admin.projects.move-down',$p) }}">@csrf<button class="btn btn-sm btn-outline-secondary"><i class="fa-solid fa-arrow-down"></i> เลื่อนลง</button></form></div></td><td><b>🏢 {{ $p->title_th }}</b><br><small>{{ $p->title_en }}</small><br><span class="text-muted">ประเภทงาน: {{ optional($p->service)->title_th ?: '-' }}</span><br><span class="text-muted">สถานที่: {{ $p->location_th ?: '-' }}</span><br><span class="text-muted">วันที่ดำเนินงาน: {{ optional($p->project_date ?: $p->finished_at ?: $p->started_at)->format('d/m/Y') ?: '-' }}</span></td><td>รูปทั้งหมด {{ $p->images->count() }} รูป<br><span class="badge text-bg-secondary">ก่อนทำ {{ $p->images->where('image_type','before')->count() }}</span> <span class="badge text-bg-info">ระหว่างทำ {{ $p->images->where('image_type','progress')->count() }}</span> <span class="badge text-bg-warning">หลังทำ {{ $p->images->where('image_type','after')->count() }}</span></td><td>{{ $p->status }}<br>{{ $p->is_active?'แสดง':'ซ่อน' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.projects.edit',$p) }}"><i class="fa-solid fa-pen"></i> แก้ไข</a><form method="post" action="{{ route('admin.projects.destroy',$p) }}" class="d-inline">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('ลบ?')"><i class="fa-solid fa-trash"></i> ลบ</button></form></td></tr>@endforeach
</table>{{ $projects->links() }}</div>
@endsection
