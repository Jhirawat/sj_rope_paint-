@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">พนักงาน</h1><p class="text-muted mb-0">จัดการหัวหน้าช่าง ช่างรายวัน ผู้ช่วย/กรรมกร บัญชี และบัญชี Login พนักงาน</p></div><div class="d-flex gap-2"><a href="{{ route('admin.staff.pay-rates') }}" class="btn btn-outline-primary"><i class="fa-solid fa-coins"></i> ตั้งค่าค่าแรง</a><a href="{{ route('admin.staff.create') }}" class="btn btn-sj"><i class="fa-solid fa-plus"></i> เพิ่มพนักงาน</a></div></div>
<div class="card p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>รูป/ชื่อ</th><th>Login</th><th>ประเภท</th><th>ค่าแรง/วัน</th><th>สถานะ</th><th class="text-end">จัดการ</th></tr></thead><tbody>@foreach($employees as $e)<tr><td><div class="d-flex gap-2 align-items-center">@if($e->profile_photo)<img src="{{ asset('storage/'.$e->profile_photo) }}" class="avatar-sm">@else<div class="avatar-sm bg-light d-flex align-items-center justify-content-center"><i class="fa-solid fa-user text-muted"></i></div>@endif<div><strong>{{ $e->name }}</strong><div class="text-muted small">{{ $e->nickname }} {{ $e->position_note }}</div></div></div></td><td><code>{{ $e->user?->username ?: '-' }}</code><div class="small text-muted">PIN 4-6 หลัก</div></td><td>{{ ['head_technician'=>'หัวหน้าช่าง','daily_technician'=>'ช่างรายวัน','helper'=>'ผู้ช่วย/กรรมกร','accounting_admin'=>'บัญชี/เลขา/ผู้จัดการ','owner'=>'เจ้าของกิจการ'][$e->employee_type] ?? $e->employee_type }}</td><td>{{ number_format($e->daily_wage) }} บาท</td><td>{!! $e->is_active ? '<span class="badge bg-success">ใช้งาน</span>' : '<span class="badge bg-secondary">ปิดใช้งาน</span>' !!}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.staff.edit',$e) }}"><i class="fa-solid fa-pen"></i> แก้ไข</a></td></tr>@endforeach</tbody></table></div>{{ $employees->links() }}</div>
@endsection
