@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h3 mb-0">ตั้งค่าค่าแรง</h1><a href="{{ route('admin.staff.index') }}" class="btn btn-light">กลับพนักงาน</a></div>
<div class="row g-4"><div class="col-lg-5"><div class="card p-4"><h5>เพิ่มค่าแรง</h5><form method="post" action="{{ route('admin.pay-rates.store') }}">@csrf<div class="mb-3"><label class="form-label">ชื่อ</label><input name="name" class="form-control" placeholder="เช่น ค่าแรง 500 บาท"></div><div class="mb-3"><label class="form-label">จำนวนเงิน *</label><input type="number" name="amount" class="form-control" required></div><div class="mb-3"><label class="form-label">ลำดับ</label><input type="number" name="sort_order" class="form-control" value="0"></div><input type="hidden" name="is_active" value="1"><button class="btn btn-sj">เพิ่ม</button></form></div></div><div class="col-lg-7"><div class="card p-3"><table class="table align-middle"><thead><tr><th>ชื่อ</th><th>เงิน</th><th>ใช้งาน</th><th></th></tr></thead><tbody>@foreach($payRates as $rate)<tr><form method="post" action="{{ route('admin.pay-rates.update',$rate) }}">@csrf @method('PATCH')<td><input name="name" class="form-control" value="{{ $rate->name }}"></td><td><input type="number" name="amount" class="form-control" value="{{ $rate->amount }}"></td><td><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked($rate->is_active)></td><td class="text-end"><input type="hidden" name="sort_order" value="{{ $rate->sort_order }}"><button class="btn btn-sm btn-outline-primary">บันทึก</button></td></form></tr>@endforeach</tbody></table></div></div></div>
@endsection
