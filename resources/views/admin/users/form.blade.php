@extends('layouts.admin')
@section('content')<h1>{{ $user->exists?'แก้ไขผู้ใช้':'เพิ่มผู้ใช้' }}</h1><form method="post" action="{{ $user->exists?route('admin.users.update',$user):route('admin.users.store') }}" class="card p-4">@csrf @if($user->exists) @method('patch') @endif<div class="row g-3"><div class="col-md-6"><label>ชื่อ</label><input name="name" class="form-control" value="{{ old('name',$user->name) }}" required></div><div class="col-md-6"><label>Email</label><input name="email" type="email" class="form-control" value="{{ old('email',$user->email) }}" required></div><div class="col-md-6"><label>Password</label><input name="password" type="password" class="form-control" @if(!$user->exists) required @endif></div><div class="col-md-6"><label>Role</label><select name="role" class="form-select">@foreach(['super_admin','admin','staff'] as $r)<option @selected(old('role',$user->role)==$r)>{{ $r }}</option>@endforeach</select></div><div class="col-md-6"><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$user->is_active ?? true))> เปิดใช้งาน</label></div><div><button class="btn btn-sj">บันทึก</button></div></div></form>@endsection
